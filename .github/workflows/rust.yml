name: Rust builds

on:
  push:
    branches: [main]
    tags:
      - v*
  pull_request:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: write

jobs:
  create-release:
    needs:
     - build
     - nixbuild
    runs-on: ubuntu-latest
    if: github.ref_type == 'tag'
    steps:
      - name: Download build artifacts
        uses: actions/download-artifact@v8
        with:
          path: artifacts
      - name: Rename and move artifacts
        run: |
          for dir in artifacts/*/
          do
            dir=${dir%*/} # remove the trailing "/"
            for file in $dir/*
            do 
              file=${file##*/} # remove the path before the filename
              base=${file%.*} # remove the extension
              ext=${file#"$base"} # remove the filename before the extension
              echo "moving ${dir}/${file} to artifacts/${file%%.*}-${dir##*/}${ext}"
              mv "${dir}/${file}" "artifacts/${file%%.*}-${dir##*/}${ext}"
            done
            rm -r "${dir}"
          done
      - name: release
        uses: ncipollo/release-action@v1
        id: create_release
        with:
          generateReleaseNotes: true
          artifacts: "artifacts/*"
  nixbuild:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6
    - name: Generate dex_key.rs
      run: echo "${{ secrets.DEX_AES_KEYS }}" > app/src/osc/dex_key.rs
    - name: Add dex_key.rs
      run: git 'add' '--force' 'app/src/osc/dex_key.rs'
    - uses: cachix/install-nix-action@v31
      with:
        nix_path: nixpkgs=channel:nixos-25.11
    - uses: DeterminateSystems/magic-nix-cache-action@v13
    - run: nix-build
    - name: remove dex_key.rs
      run: git 'rm' '--cached' 'app/src/osc/dex_key.rs'
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        rust: [stable]
        target: [x86_64-unknown-linux-gnu, x86_64-pc-windows-msvc, i686-pc-windows-msvc, x86_64-apple-darwin, aarch64-apple-darwin] #x86_64-unknown-linux-musl,
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            name: linux_64-bit
            features: "file_dialog,tray"
          #- os: ubuntu-latest
          #  target: x86_64-unknown-linux-musl
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            name: windows_64-bit
            features: "file_dialog,tray"
          - os: windows-latest
            target: i686-pc-windows-msvc
            name: windows_32-bit
            features: "file_dialog,tray"
#FIXME(tray-icon): Mac is known broken currently: https://github.com/tauri-apps/muda/pull/341#issuecomment-4504194244
          - os: macos-latest
            target: x86_64-apple-darwin
            name: macos_64-bit
            features: "file_dialog"
          - os: macos-latest
            target: aarch64-apple-darwin
            name: macos_aarch64
            features: "file_dialog"
      fail-fast: false
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
      - name: Generate dex_key.rs
        run: echo "${{ secrets.DEX_AES_KEYS }}" > app/src/osc/dex_key.rs
      - name: Install Rust
        uses: moonrepo/setup-rust@v1
        with:
          cache-target: release
          targets: ${{ matrix.target }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Linux Specific libs install
        if: matrix.os == 'ubuntu-latest'
        uses: awalsh128/cache-apt-pkgs-action@v1
        with:
          packages: libglib2.0-dev libgtk-3-dev libgtk-4-dev libxdo-dev libappindicator3-dev
          execute_install_scripts: true
      - name: Run build
        run: cargo build --target ${{ matrix.target }} --release --features ${{ matrix.features }} --package dex_protect_osc_rs --bin dex_protect_osc_rs
      - name: Upload build artifact
        uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.name }}
          path: |
            target/${{ matrix.target }}/release/
            !target/${{ matrix.target }}/release/deps
            !target/${{ matrix.target }}/release/build
            !target/${{ matrix.target }}/release/.fingerprint
            !target/${{ matrix.target }}/release/examples
            !target/${{ matrix.target }}/release/incremental
            !target/${{ matrix.target }}/release/.cargo-lock
            !target/${{ matrix.target }}/release/*.d
            !target/${{ matrix.target }}/release/*.pdb
